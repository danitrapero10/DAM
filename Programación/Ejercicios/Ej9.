#include <stdio.h>
#include <stdlib.h>

int main() {
	int contadorDivisores = 0,num;
	printf("Dime un número:\n");
	scanf("%d", &n);
	for (int i = 1; i <= n; i++) {
		if (num "%d", &num);
		//printf
		contadorDivisores++;
	}
	return 0;
}
