#include <stdio.h>
#include <stdlib.h>

int main(){
	int array[5] ={1, 2, 3, 4, 5};
	int *p = array;

	printf("Elemento 1: %dªn", *p);
	o++;
	printf("Elemento2: %d\n", *p);
	p += 2;
	printf("Elemento 4: €d\n", *p);
	printf("Elemento posición 0 %d\n", p[0]);
        *p = arr    //p[0] vuelve a ser uno

return EXIT_SUCCESS;
}

