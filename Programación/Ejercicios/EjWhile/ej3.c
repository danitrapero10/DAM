#include <stdio.h>
#include <stdlib.h>
int main() {
	int numero = 0;
	int cociente = 0;
        int resto = 0;
  do{ cociente = numero / 10;
	  resto = resto * 10 +(numero % 10);
	  numero = cociente;
     }	while (num != 0);
}
        printf ("El resultado de la suma de los números es %d\n", suma);
return 0;
}
